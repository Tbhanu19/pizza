# Avoid ESLint "Plugin react was conflicted" on Windows when path casing differs (e.g. hunts vs Hunts)
SKIP_PREFLIGHT_CHECK=true
# Disable ESLint during build so path casing conflict does not fail compilation
DISABLE_ESLINT_PLUGIN=true
